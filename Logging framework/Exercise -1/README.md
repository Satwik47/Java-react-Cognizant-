# Logging example (SLF4J + Logback)

Build and run (requires JDK and Maven installed):

```powershell
mvn package -DskipTests
java -jar target/logging-example-1.0-SNAPSHOT.jar
```

If the shaded jar name differs, check `target/` for `-shaded.jar`.
