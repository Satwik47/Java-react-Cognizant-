Hibernate XML mapping sample

Run with Maven:

```bash
mvn clean compile exec:java -Dexec.mainClass="com.example.hibernate.MainApp"
```

This project demonstrates object-relational mapping using `hibernate.cfg.xml` and `*.hbm.xml` mapping files against an H2 in-memory database.
