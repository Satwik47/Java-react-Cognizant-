================================================================================
SPRING DATA JPA - HANDS ON 7 - SAMPLE OUTPUT
Add a New Country using Spring Boot + Spring Data JPA + Hibernate
================================================================================

When you run OrmLearnApplication.java (or mvn spring-boot:run), you should see
output similar to the following in your console:

================================================================================

  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_|\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::        (v2.3.0.RELEASE)

2026-07-04 11:50:00.123  INFO 12345 --- [  restartedMain] c.c.ormlearn.OrmLearnApplication         : Starting OrmLearnApplication v0.0.1-SNAPSHOT
2026-07-04 11:50:01.456  INFO 12345 --- [  restartedMain] o.hibernate.dialect.Dialect              : HHH000400: Using dialect: org.hibernate.dialect.MySQL8Dialect
2026-07-04 11:50:02.789  INFO 12345 --- [  restartedMain] c.c.ormlearn.OrmLearnApplication         : Started OrmLearnApplication in 3.456 seconds

Start

Hibernate:
    insert
    into
        country
        (co_name, co_code)
    values
        (?, ?)

Hibernate:
    select
        c1_0.co_code,
        c1_0.co_name
    from
        country c1_0
    where
        c1_0.co_code=?

Country [code=IN, name=India]

End

================================================================================

DATABASE VERIFICATION (MySQL Workbench)
================================================================================

Execute:

USE ormlearn;

SELECT * FROM country;

Output:

+---------+-------+
| co_code | co_name |
+---------+-------+
| IN      | India   |
+---------+-------+

================================================================================

KEY POINTS IN THE OUTPUT:

1. Spring Boot Initialization:
   - Application context is created
   - HikariCP connection pool connects to MySQL (ormlearn database)
   - Hibernate dialect is configured for MySQL8
   - JPA EntityManagerFactory is initialized
   - Tomcat server starts on port 8080

2. Application Execution (testAddCountry):
   - "Start" is printed before the insert operation
   - A new Country object is created with code "IN" and name "India"
   - countryService.addCountry() persists the record to the database

3. Hibernate INSERT SQL:
   - Hibernate generates and executes an INSERT into the country table
   - Parameters: co_name = "India", co_code = "IN"

4. Hibernate SELECT SQL:
   - countryService.findCountryByCode("IN") retrieves the saved record
   - SELECT query runs with co_code = "IN"

5. Test Method Result:
   - Country [code=IN, name=India] is printed to the console
   - "End" is printed after verification

6. Database Verification:
   - SELECT * FROM country confirms the row exists in MySQL
   - co_code = IN, co_name = India

7. No Errors:
   - The application completes successfully without exceptions
   - Connection to MySQL database is successful
   - Insert and retrieve operations work correctly

================================================================================
