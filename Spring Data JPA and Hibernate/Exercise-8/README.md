# Employee Management System — Projections (Exercise 8)

This is a minimal Spring Boot project demonstrating Spring Data JPA projections (interface-based).

Run with:

```
mvn spring-boot:run
```

Outputs from a sample run are included in `output.txt`.
