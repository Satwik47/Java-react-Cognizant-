# Observer Pattern Demo

Simple Java demo implementing the Observer Pattern for a stock market.

To compile and run (from repository root):

```powershell
javac -d out src/observer/*.java
java -cp out observer.ObserverDemo
```
